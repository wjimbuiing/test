import styles from './app.module.scss'
import Input from "../Input/Input";

function App() {
  return <section className={`${styles.contentC}`}>
      <Input></Input>
  </section>;
}

export default App;
